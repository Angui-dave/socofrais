import 'package:flutter/material.dart';

class ValidePaiementView extends StatelessWidget {
  const ValidePaiementView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}